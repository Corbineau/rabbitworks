const express = require('express');
const router = express.Router();

router.get('/', (req,res) => {
    //find the swappable react element in app and put in the correct components
});

module.exports = router;